name: Build-Main

on:
  push:
    branches: [ main ]
jobs:
  create-dev:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: Create development branch and build
      run: .\scripts\Check-CommitMessage -GithubContext ${{ github }}
      shell: pwsh
